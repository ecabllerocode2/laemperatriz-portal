{
  "buildCommand": "pnpm --filter @emperatriz/portal build",
  "outputDirectory": "apps/portal/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
